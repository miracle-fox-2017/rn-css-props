const initialState = {
  letter_used: [],
  turn_left: [],
  gamestatus: ''
}
export const gameOn = (state = initialState, action) => {
  switch (action.type) {
    case '':
      return state
    default:
      return state
  }
}